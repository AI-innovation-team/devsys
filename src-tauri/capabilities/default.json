{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "DevSys 主窗口默认能力：核心 API（invoke / event）。",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-set-fullscreen",
    "core:window:allow-is-fullscreen",
    "dialog:allow-open",
    "dialog:allow-save"
  ]
}
