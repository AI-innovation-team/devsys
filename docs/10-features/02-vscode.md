# VS Code

在**服务器**页点某台机器的 **VS Code**,在浏览器里打开该机上的 code-server 实例,像本地 VS Code 一样编辑代码、开集成终端。

## 特性

- 实例**常驻**:关掉网页下次回来,打开的文件夹、编辑器状态都还在。
- 每台机器独立实例,互不干扰(按主机名隔离配置目录)。
- 集成终端同样预置了 Nerd Font 字形。

## 提示

- 首次打开可能稍慢,需要拉起 code-server 进程,请耐心等几秒。
- 插件、设置存在你在该机的 home 下,与本地 VS Code 相互独立。
